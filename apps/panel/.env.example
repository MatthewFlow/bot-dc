# ── PANEL (Next.js) ────────────────────────────────────────────────────────
# Bazowy URL API. Wbudowywany w bundle przy `next build` (prefiks NEXT_PUBLIC_).
#
# Dev:        http://localhost:3002
# Produkcja:  /api  (same-origin za reverse proxy — ustawiane w docker-compose)
NEXT_PUBLIC_API_URL=http://localhost:3002
